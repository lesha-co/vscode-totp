import { window, workspace } from "vscode";

export const showInputBox = window.showInputBox;
export const showQuickPick = window.showQuickPick;
export const createQuickPick = window.createQuickPick;
export const showTextDocument = window.showTextDocument;
export const createStatusBarItem = window.createStatusBarItem;
export const showInformationMessage = window.showInformationMessage;
export const showOpenDialog = window.showOpenDialog;
export const showErrorMessage = window.showErrorMessage;
export const openTextDocument = workspace.openTextDocument;
